# Omitted Large Files

`sair_label_progress.jsonl` was generated during the live sync but intentionally
omitted from the repo because it is a large regenerable all-label API payload.
The committed sync summary and progress summary retain the coverage metrics
used by this run.
